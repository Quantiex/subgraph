{
  "name": "tidalx-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ test",
    "create-local": "graph create --node http://localhost:8020/ tidalx-test",
    "remove-local": "graph remove --node http://localhost:8020/ tidalx-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tidalx-test",
    "test": "graph test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Quantiex/subgraph.git"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.81.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
